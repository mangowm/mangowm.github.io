import { LayoutGridIcon } from "lucide-react";
import type { SectionMeta } from "@/lib/section-types";

export const layoutMeta: SectionMeta = {
  id: "layout",
  label: "Layout",
  icon: <LayoutGridIcon />,
  keywords: ["tiling", "master", "stack", "arrangement", "dwindle", "scroller"],
};
