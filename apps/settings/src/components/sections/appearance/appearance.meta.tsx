import { PaintbrushIcon } from "lucide-react";
import type { SectionMeta } from "@/lib/section-types";

export const appearanceMeta: SectionMeta = {
  id: "appearance",
  label: "Appearance",
  icon: <PaintbrushIcon />,
  keywords: ["theme", "visual", "style", "colors", "effects"],
};
